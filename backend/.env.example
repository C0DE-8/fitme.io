DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=meal_planner

EMAIL_USER=davisam0107@gmail.com
EMAIL_PASS=ogwixesggnixieog
OTP_EXPIRY_MINUTES=10

JWT_SECRET=this_is_gods_not_man_990_555_heaven

# Google Gemini (https://aistudio.google.com/apikey)
GEMINI_API_KEY=
# Optional fallback names the loader also accepts:
# GOOGLE_GENERATIVE_AI_API_KEY=
# Optional: default in code is gemini-2.0-flash; older names like gemini-1.5-flash may 404 on v1beta
GEMINI_MODEL=gemini-2.0-flash
# Optional: HTTP timeout (ms) and retries for transient network errors
GEMINI_REQUEST_TIMEOUT_MS=120000
GEMINI_MAX_RETRIES=3
